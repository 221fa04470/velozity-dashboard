# Where the browser opens the WebSocket. Vercel can't proxy WebSockets, so this points straight at the API host.
VITE_SOCKET_URL=http://localhost:4000

# Leave unset to call the API at the relative path /api (Vite proxy in dev, Vercel rewrite in production).
# VITE_API_URL=
